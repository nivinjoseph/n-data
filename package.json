{
    "name": "@nivinjoseph/n-data",
    "version": "2.0.3",
    "description": "Data access library for Postgres based on Knex",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "ts-compile": "tsc -p .",
        "ts-lint": "tslint -p . -c tslint.json",
        "ts-build": "npm run ts-compile && npm run ts-lint",
        "ts-build-dist": "npm run ts-build && tsc -p ./dist",
        "clean-src": "find ./src -name '*.js' -delete -o -name '*.map' -delete",
        "clean-test": "find ./test -name '*.js' -delete -o -name '*.map' -delete",
        "setup-db-server": "docker run --name test-postgres -p 5432:5432 -e POSTGRES_PASSWORD=p@ssw0rd -d postgres:10.5 && sleep 5",
        "setup-db": "docker run -it --rm -e PGPASSWORD=p@ssw0rd postgres:10.5 psql -h 172.17.0.1 -U postgres -c 'create database testdb;'",
        "pretest": "npm run ts-build && npm run setup-db-server && npm run setup-db",
        "test": "mocha --reporter spec --ui tdd --require ts-node/register './test/**/*.test.js' || true",
        "posttest": "docker stop test-postgres && docker rm test-postgres",
        "pretest-ci": "npm run pretest",
        "test-ci": "mocha --reporter spec --ui tdd --require ts-node/register './test/**/*.test.js'",
        "publish-package": "npm run ts-build-dist && git add . && git commit -m 'preparing to publish new version' && npm version patch && git push && npm publish --access=public"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/nivinjoseph/n-data.git"
    },
    "keywords": [
        "data"
    ],
    "author": "NiviN",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/nivinjoseph/n-data/issues"
    },
    "homepage": "https://github.com/nivinjoseph/n-data#readme",
    "devDependencies": {
        "@types/knex": "0.16.1",
        "@types/mocha": "^5.2.5",
        "@types/node": "^8.9.5",
        "mocha": "^5.2.0",
        "ts-node": "^7.0.1",
        "tslint": "^5.11.0",
        "typescript": "^3.1.1"
    },
    "dependencies": {
        "@nivinjoseph/n-defensive": "^1.0.26",
        "@nivinjoseph/n-exception": "^1.0.17",
        "@nivinjoseph/n-ext": "^1.1.5",
        "@nivinjoseph/n-ject": "^1.1.5",
        "@nivinjoseph/n-util": "^1.0.28",
        "knex": "^0.16.3",
        "pg": "^7.9.0",
        "treeize": "^2.1.2"
    }
}
